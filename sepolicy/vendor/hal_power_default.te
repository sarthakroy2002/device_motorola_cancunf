# Allow libperfmgr to control cpu frequencies
rw_dir_file(hal_power_default, proc_cpufreq)

# Allow libperfmgr to control dt2w
rw_dir_file(hal_power_default, vendor_sysfs_touchpanel)

# Sensors
rw_dir_file(hal_power_default, sysfs_sensor)
